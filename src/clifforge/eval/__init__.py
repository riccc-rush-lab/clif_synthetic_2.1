"""Evaluation stage — utility (TSTR), privacy, and fidelity metrics (R19, R20, R24).

Imported only when the ``eval`` extra is installed; the generators never import
this package, keeping the heavier ML dependency tree out of the generate path.
"""
